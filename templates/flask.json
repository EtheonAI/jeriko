{
  "name": "flask",
  "description": "Flask Python web app",
  "command": "mkdir -p {{name}} && cd {{name}} && python3 -m venv venv && ./venv/bin/pip install flask",
  "postInstall": "",
  "devCommand": "flask run",
  "devPort": 5000
}
